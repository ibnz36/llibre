# llibre

(early wip) `llibre` is a cli tool for managing a local book library.

## Features

- [ ] Store books by category
    - [X] (lib) Add book function
    - [ ] Add Books to category through interactive CLI
    - [ ] Add Books to category through command args
- [ ] Config file
    - [ ] (lib) Full config options
    - [ ] Edit through interactive CLI
    - [ ] Edit through command args
